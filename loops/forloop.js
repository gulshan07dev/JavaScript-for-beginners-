// syntax
// for(initialisation; condition; updation) {
//   code
// }

// print table using for loop
let table = 5;
for(let i = 1; i <= 10; i++) {
    console.log(`${table} x ${i} = ${table * i}`);
}