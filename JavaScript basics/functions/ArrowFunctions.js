// An arrow function is a shorthand syntax for writing function expression. it starts with the parameters in parantheses, followed by the arrow (=>) and the function body. for example

let add = (a, b) => {
    return a+b;
};

console.log(add(3, 7));